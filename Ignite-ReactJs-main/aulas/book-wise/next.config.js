/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'github.com',
      'book-wise.vercel.app',
      'images.unsplash.com',
    ],
  },
}

module.exports = nextConfig
